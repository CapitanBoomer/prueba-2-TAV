import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Diego Muñoz prueba 2 tav',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
